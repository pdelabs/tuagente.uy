INSTANCE=tuagente
PORT_GATEWAY=8652
PORT_ADAPTER=8653
# The lab's Phoenix, published on the host's loopback by docker-compose.yml.
CORE_OTEL_ENDPOINT=http://host.docker.internal:6006/v1/traces
CORE_OTEL_INCLUDE_CONTENT=1
