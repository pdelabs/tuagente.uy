__pycache__/
*.pyc
.DS_Store

# Copies of the old SOUL that install-soul.sh --replace leaves behind: they
# are a client's data (their identity, their naming block), not the kit's.
soul-backups/

# Copies of the previous config.yaml that skills-knob.py --apply leaves
# behind: they belong to a specific agent, not the kit.
config-backups/
dist/

# Deps of the drawing CLI (tools/draw-agentito.mjs); npm install brings them.
tools/node_modules/
