# Las UNICAS skills del motor que quedan prendidas en un agente de cliente.
# Una por linea; lo que empieza con # es comentario.
#
# El resto se apaga con `skills.disabled` en config.yaml — la lista la genera
# tools/perilla-skills.py, no se escribe a mano. Este archivo es la excepcion,
# o sea la politica: lo leen el generador y agente-check.py.
#
# Por que estas cuatro y no otras: son las que leen los documentos que el
# cliente manda por el chat (una planilla, un PDF, un Word, la foto de un
# remito). Sin ellas el agente recibe un archivo y no puede abrirlo. Ninguna
# habla hacia afuera, ninguna publica y ninguna toca sistemas: leen lo que ya
# te mandaron.
xlsx
pdf
ocr-and-documents
# En una pyme los contratos, los briefs y las propuestas van en Word, no en PDF.
# Sin esta, el agente pide "mandámelo en PDF" y el trabajo se lo come el cliente.
docx
